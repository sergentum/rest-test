# Use postgres/example user/password credentials
version: '3.1'

services:

  test-app:
    image: sergentum/rest-test
    ports:
      - 8765:8080

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example